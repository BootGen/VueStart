server {
    listen 80;

    location /sites {
        root /var/www;
        try_files $uri $uri/index.html;
    }

    location / {
        root /var/www/startvue;
        try_files $uri /index.html;
    }
}